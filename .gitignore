# Database files
*.db
*.db.bak
*.db-journal
*.db-shm
*.db-wal

# Database backup snapshots (real health data — never commit)
backups/
biotracking_backup_*

# Config with secrets (API keys, secret_key, passcode)
config.json

# Ephemeral alert state
config/flare_alert_state.json
config/uv_alert_state.json

# Personal health data
*.csv
flare_data_lab_results/
apple_health_export.csv

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Images (user-uploaded)
images/

# Root-level PNGs — source files and analysis script outputs.
# (Actual app favicons live in images/favicon/ and are served by Flask.)
# The leading slash anchors this to repo root so subfolder PNGs still ship.
/*.png

# Claude Code per-project memory (auto-memory working notes; private)
memory/
MEMORY.md

# Working scratch / "kitchen" notes — never public
notes/

# Python venv
.venv/
biotracking.db*
venv/
.venv-backup/

# Imported clinical records — PHI, never commit
imports/

# Uploaded clinical PDFs — PHI, never commit
documents/
